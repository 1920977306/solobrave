diff --git a/office-v3.html b/office-v3.html
index 994fab5..822c8aa 100755
--- a/office-v3.html
+++ b/office-v3.html
@@ -2,7 +2,7 @@
 <meta charset="UTF-8">
 <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
 <meta http-equiv="Pragma" content="no-cache">
-<meta http-equiv="Expires" content="0"><!-- v20260525c --><meta name="viewport" content="width=device-width, initial-scale=1.0">
+<meta http-equiv="Expires" content="0"><!-- v20260526a --><meta name="viewport" content="width=device-width, initial-scale=1.0">
 <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Crect width='100' height='100' rx='20' fill='%23FF6B35'/%3E%3Ctext x='50' y='68' text-anchor='middle' font-size='55' font-weight='bold' fill='white' font-family='-apple-system,BlinkMacSystemFont,sans-serif'%3EQK%3C/text%3E%3C/svg%3E?v=2">
 <title>全可AI Office</title><style>
 /* ====== 全局重置 & 基础变量 ====== */
@@ -2383,24 +2383,24 @@ body{font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','SF Pro Text'
 
 <script>
 // ===== 数据打通：认证与 API =====
-const API_BASE = '';  // 与主页共用后端
+var API_BASE = '';  // 与主页共用后端
 
 // 获取认证头
 function getAuthHeaders() {
-  const token = localStorage.getItem('sb_auth_token');
+  var token = localStorage.getItem('sb_auth_token');
   return token ? { 'Authorization': 'Bearer ' + token } : {};
 }
 
 // 带认证的 Fetch，自动处理 401
 async function authFetch(url, options = {}) {
   options.headers = { ...options.headers || {}, ...getAuthHeaders() };
-  const r = await fetch(url, options);
+  var r = await fetch(url, options);
   if (r.status === 401) {
     localStorage.removeItem('sb_auth_token');
     localStorage.removeItem('sb_current_user');
     // 跳转回主页登录
     showToast('请先登录，正在跳转...', 'error');
-    setTimeout(() => { location.href = './index.html'; }, 1500);
+    setTimeout(function() { location.href = './index.html'; }, 1500);
     throw new Error('Unauthorized');
   }
   return r;
@@ -2408,10 +2408,10 @@ async function authFetch(url, options = {}) {
 
 // 检查登录状态
 function checkOfficeAuth() {
-  const token = localStorage.getItem('sb_auth_token');
+  var token = localStorage.getItem('sb_auth_token');
   if (!token || token === 'null') {
     showToast('请先在主页登录', 'error');
-    setTimeout(() => { location.href = './index.html'; }, 1500);
+    setTimeout(function() { location.href = './index.html'; }, 1500);
     return false;
   }
   return true;
@@ -2509,10 +2509,10 @@ function updateOpenClawStatus(connected){
 
 
 // ===== 数据定义 (支持 Mock 和 Real) =====
-let employees = [];
-let activities = [];
+var employees = [];
+var activities = [];
 
-const activitiesFallback = [
+var activitiesFallback = [
   { id:1, name:'Lucy', initial:'L', gradient:['#FF6B9D','#C44FE2'], status:'thinking', text:'正在分析用户需求...', time:'刚刚' },
   { id:2, name:'Cute', initial:'C', gradient:['#FF9500','#FF2D55'], status:'thinking', text:'设计评审进行中...', time:'刚刚' },
   { id:3, name:'Eric', initial:'E', gradient:['#5AC8FA','#34C759'], status:'done', text:'✅ 前端组件库更新完成', time:'2分钟前' },
@@ -2595,8 +2595,8 @@ async function getAgentIdentity(agentId) {
   }
 
   try {
-    const identity = await openclaw.agent_identity_get(agentId);
-    const sessions = await openclaw.listSessions();
+    var identity = await openclaw.agent_identity_get(agentId);
+    var sessions = await openclaw.listSessions();
     
     return {
       model: (identity.config && identity.config.model) || identity.model || 'unknown',
@@ -2614,7 +2614,7 @@ async function getAgentIdentity(agentId) {
 
 // Load available models
 async function loadModels() {
-  const select = document.getElementById('modelSelect');
+  var select = document.getElementById('modelSelect');
   if (!select) return;
 
   // Show loading
@@ -2623,79 +2623,55 @@ async function loadModels() {
 
   if (!openclaw || !openclaw._connected) {
     // Use fallback models
-    select.innerHTML = `
-      <option value="glm-5">glm-5 (BigModel)</option>
-      <option value="gpt-4">gpt-4 (OpenAI)</option>
-      <option value="claude-3">claude-3 (Anthropic)</option>
-    `;
+    select.innerHTML = "\n      <option value=\"glm-5\">glm-5 (BigModel)</option>\n      <option value=\"gpt-4\">gpt-4 (OpenAI)</option>\n      <option value=\"claude-3\">claude-3 (Anthropic)</option>\n    ";
     select.disabled = false;
     return;
   }
 
   try {
-    const models = await openclaw.listModels();
+    var models = await openclaw.listModels();
     if (Array.isArray(models) && models.length > 0) {
-      select.innerHTML = models.map(m => 
-        `<option value="${m.id || m.name}">${m.name || m.id}</option>`
-      ).join('');
+      select.innerHTML = models.map(function(m) {
+        return "<option value=\"" + (m.id || m.name) + "\">" + (m.name || m.id) + "</option>";
+      }).join('');
     } else {
       // Fallback
-      select.innerHTML = `
-        <option value="glm-5">glm-5 (BigModel)</option>
-        <option value="gpt-4">gpt-4 (OpenAI)</option>
-        <option value="claude-3">claude-3 (Anthropic)</option>
-      `;
+      select.innerHTML = "\n        <option value=\"glm-5\">glm-5 (BigModel)</option>\n        <option value=\"gpt-4\">gpt-4 (OpenAI)</option>\n        <option value=\"claude-3\">claude-3 (Anthropic)</option>\n      ";
     }
     select.disabled = false;
   } catch (err) {
     console.error('[OpenClaw] Failed to load models:', err);
-    select.innerHTML = `
-      <option value="glm-5">glm-5 (BigModel)</option>
-      <option value="gpt-4">gpt-4 (OpenAI)</option>
-      <option value="claude-3">claude-3 (Anthropic)</option>
-    `;
+    select.innerHTML = "\n      <option value=\"glm-5\">glm-5 (BigModel)</option>\n      <option value=\"gpt-4\">gpt-4 (OpenAI)</option>\n      <option value=\"claude-3\">claude-3 (Anthropic)</option>\n    ";
     select.disabled = false;
   }
 }
 
 // Load channel status
 async function loadChannels() {
-  const container = document.getElementById('channelTabs');
+  var container = document.getElementById('channelTabs');
   if (!container) return;
 
   if (!openclaw || !openclaw._connected) {
     // Use fallback channels
-    container.innerHTML = `
-      <div class="channel-tab active" data-channel="dingtalk" onclick="switchChannelTab(this)">钉钉 <span class="channel-check">✓</span></div>
-      <div class="channel-tab" data-channel="feishu" onclick="switchChannelTab(this)">飞书</div>
-      <div class="channel-tab" data-channel="agentlink" onclick="switchChannelTab(this)">AgentLink <span class="channel-check">✓</span></div>
-      <div class="channel-tab" data-channel="telegram" onclick="switchChannelTab(this)">Telegram <span class="channel-check">✓</span></div>
-    `;
+    container.innerHTML = "\n      <div class=\"channel-tab active\" data-channel=\"dingtalk\" onclick=\"switchChannelTab(this)\">钉钉 <span class=\"channel-check\">✓</span></div>\n      <div class=\"channel-tab\" data-channel=\"feishu\" onclick=\"switchChannelTab(this)\">飞书</div>\n      <div class=\"channel-tab\" data-channel=\"agentlink\" onclick=\"switchChannelTab(this)\">AgentLink <span class=\"channel-check\">✓</span></div>\n      <div class=\"channel-tab\" data-channel=\"telegram\" onclick=\"switchChannelTab(this)\">Telegram <span class=\"channel-check\">✓</span></div>\n    ";
     return;
   }
 
   try {
-    const status = await openclaw.send('channels.status', {});
+    var status = await openclaw.send('channels.status', {});
     if (status && typeof status === 'object') {
-      const channels = ['dingtalk', 'feishu', 'agentlink', 'telegram'];
-      const channelNames = { dingtalk: '钉钉', feishu: '飞书', agentlink: 'AgentLink', telegram: 'Telegram' };
+      var channels = ['dingtalk', 'feishu', 'agentlink', 'telegram'];
+      var channelNames = { dingtalk: '钉钉', feishu: '飞书', agentlink: 'AgentLink', telegram: 'Telegram' };
       
-      container.innerHTML = channels.map(ch => {
-      const enabled = (status[ch] && status[ch].enabled) || false;
-        return `<div class="channel-tab${enabled ? ' active' : ''}" data-channel="${ch}" onclick="switchChannelTab(this)">
-          ${channelNames[ch]} ${enabled ? '<span class="channel-check">✓</span>' : ''}
-        </div>`;
+      container.innerHTML = channels.map(function(ch) {
+      var enabled = (status[ch] && status[ch].enabled) || false;
+        return "<div class=\"channel-tab" + (enabled ? ' active' : '') + "\" data-channel=\"" + (ch) + "\" onclick=\"switchChannelTab(this)\">\n          " + (channelNames[ch]) + " " + (enabled ? '<span class="channel-check">✓</span>' : '') + "\n        </div>";
       }).join('');
     }
   } catch (err) {
     console.error('[OpenClaw] Failed to load channels:', err);
     // Fallback
-    container.innerHTML = `
-      <div class="channel-tab active" data-channel="dingtalk" onclick="switchChannelTab(this)">钉钉 <span class="channel-check">✓</span></div>
-      <div class="channel-tab" data-channel="feishu" onclick="switchChannelTab(this)">飞书</div>
-      <div class="channel-tab" data-channel="agentlink" onclick="switchChannelTab(this)">AgentLink</div>
-      <div class="channel-tab" data-channel="telegram" onclick="switchChannelTab(this)">Telegram</div>
-    `;
+    container.innerHTML = "\n      <div class=\"channel-tab active\" data-channel=\"dingtalk\" onclick=\"switchChannelTab(this)\">钉钉 <span class=\"channel-check\">✓</span></div>\n      <div class=\"channel-tab\" data-channel=\"feishu\" onclick=\"switchChannelTab(this)\">飞书</div>\n      <div class=\"channel-tab\" data-channel=\"agentlink\" onclick=\"switchChannelTab(this)\">AgentLink</div>\n      <div class=\"channel-tab\" data-channel=\"telegram\" onclick=\"switchChannelTab(this)\">Telegram</div>\n    ";
   }
 }
 
@@ -2706,7 +2682,7 @@ async function toggleChannel(channelId, enable) {
     return;
   }
 
-  showToast(`${enable ? '启动' : '停止'} ${channelId}...`, 'loading');
+  showToast((enable ? '启动' : '停止') + " " + (channelId) + "...", 'loading');
 
   try {
     if (enable) {
@@ -2714,11 +2690,11 @@ async function toggleChannel(channelId, enable) {
     } else {
       await openclaw.send('channels.stop', {channel_id: channelId});
     }
-    showToast(`✅ ${channelId} ${enable ? '已启动' : '已停止'}`, 'success');
+    showToast("✅ " + (channelId) + " " + (enable ? '已启动' : '已停止'), 'success');
     await loadChannels();
   } catch (err) {
     console.error('[OpenClaw] Failed to toggle channel:', err);
-    showToast(`❌ 操作失败: ${err.message}`, 'error');
+    showToast("❌ 操作失败: " + (err.message), 'error');
   }
 }
 
@@ -2812,10 +2788,10 @@ async function saveDocContent(docName, content) {
 
 // Save model config
 async function saveModelConfig() {
-  const select = document.getElementById('modelSelect');
-  const textarea = document.getElementById('modelJson');
-      const modelId = (select && select.value);
-      const jsonConfig = (textarea && textarea.value);
+  var select = document.getElementById('modelSelect');
+  var textarea = document.getElementById('modelJson');
+      var modelId = (select && select.value);
+      var jsonConfig = (textarea && textarea.value);
 
   if (!modelId) {
     showToast('请选择模型', 'error');
@@ -2839,21 +2815,21 @@ async function saveModelConfig() {
     closeModelConfig();
   } catch (err) {
     console.error('[OpenClaw] Failed to save config:', err);
-    showToast(`❌ 保存失败: ${err.message}`, 'error');
+    showToast("❌ 保存失败: " + (err.message), 'error');
   }
 }
 
 // Test chat
 async function testChat() {
-  const textarea = document.getElementById('modelJson');
-      const testMessage = (textarea && textarea.value) || 'Hello, this is a test message.';
+  var textarea = document.getElementById('modelJson');
+      var testMessage = (textarea && textarea.value) || 'Hello, this is a test message.';
 
   if (!openclaw || !openclaw._connected) {
     showToast('🔴 离线模式：无法发送测试消息', 'error');
     return;
   }
 
-  const btn = document.querySelector('.config-test-btn');
+  var btn = document.querySelector('.config-test-btn');
   if (btn) {
     btn.disabled = true;
     btn.innerHTML = '<span class="spinner"></span> 发送中...';
@@ -2866,7 +2842,7 @@ async function testChat() {
     showToast('✅ 测试消息已发送', 'success');
   } catch (err) {
     console.error('[OpenClaw] Failed to send test:', err);
-    showToast(`❌ 测试失败: ${err.message}`, 'error');
+    showToast("❌ 测试失败: " + (err.message), 'error');
   } finally {
     if (btn) {
       btn.disabled = false;
@@ -2878,17 +2854,9 @@ async function testChat() {
 // Create new model
 function createModel() {
   showToast('🏗️ 打开模型创建表单...', 'loading');
-  const textarea = document.getElementById('modelJson');
+  var textarea = document.getElementById('modelJson');
   if (textarea) {
-    textarea.value = `{
-  "name": "my-model",
-  "provider": "custom",
-  "apiUrl": "https://api.example.com/v1",
-  "apiKey": "your-api-key",
-  "model": "gpt-4",
-  "maxTokens": 2000,
-  "temperature": 0.7
-}`;
+    textarea.value = "{\n  \"name\": \"my-model\",\n  \"provider\": \"custom\",\n  \"apiUrl\": \"https://api.example.com/v1\",\n  \"apiKey\": \"your-api-key\",\n  \"model\": \"gpt-4\",\n  \"maxTokens\": 2000,\n  \"temperature\": 0.7\n}";
   }
 }
 
@@ -3100,7 +3068,7 @@ function broadcastStatus(agentId, status) {
   }
 }
 
-const skillPresets = [
+var skillPresets = [
   {name:'brainstorm', emoji:'💡', desc:'头脑风暴'},
   {name:'debug', emoji:'🔧', desc:'系统调试'},
   {name:'tdd', emoji:'🧪', desc:'测试驱动'},
@@ -3109,7 +3077,7 @@ const skillPresets = [
   {name:'refactor', emoji:'♻️', desc:'代码重构'},
 ];
 
-const docsData = [
+var docsData = [
   {id:1, name:'产品需求文档 PRD', icon:'📋', type:'project', tags:['产品','需求'], date:'今天更新'},
   {id:2, name:'技术架构设计', icon:'🏗️', type:'tech', tags:['架构','后端'], date:'昨天'},
   {id:3, name:'UI 设计规范 v2', icon:'🎨', type:'design', tags:['设计','规范'], date:'3天前'},
@@ -3121,8 +3089,8 @@ const docsData = [
 // ===== Tab 切换 =====
 function switchTab(tab){
   // Tab 切换已简化：办公室只显示三大区域+动态面板
-  document.querySelectorAll('.nav-tab').forEach(t=>t.classList.remove('active'));
-  document.querySelectorAll('.tab-content').forEach(c=>c.classList.remove('active'));
+  document.querySelectorAll('.nav-tab').forEach(function(t) { t.classList.remove('active'); });
+  document.querySelectorAll('.tab-content').forEach(function(c) { c.classList.remove('active'); });
   if (event && event.target) event.target.classList.add('active');
   var tabEl = document.getElementById('tab-'+tab); if (tabEl) tabEl.classList.add('active');
 }
@@ -3132,10 +3100,10 @@ function animateNumber(el,target,duration){
   if(!el) return;
   duration = duration || 800;
   var start = parseInt(el.textContent)||0;
-  const startTime = performance.now();
+  var startTime = performance.now();
   function update(ct){
-    const progress = Math.min((ct-startTime)/duration,1);
-    const ease = 1-Math.pow(1-progress,3);
+    var progress = Math.min((ct-startTime)/duration,1);
+    var ease = 1-Math.pow(1-progress,3);
     el.textContent = Math.floor(start+(target-start)*ease);
     if(progress<1) requestAnimationFrame(update);
   }
@@ -3190,7 +3158,7 @@ function goToMainChat(empId) {
 
 // 渲染员工网格 - 点击跳转到主页
 function renderEmployees(){
-  const grid = document.getElementById('employeeGrid');
+  var grid = document.getElementById('employeeGrid');
   if(!grid)return;
   grid.innerHTML = employees.map(function(emp, idx){
     var avatarHtml = renderAvatar(emp, 64);
@@ -3683,36 +3651,38 @@ function sendTask(empId) {
 
 // ===== 技能系统 =====
 function renderSkills(){
-  const list = document.getElementById('skillsList');
-  const empty = document.getElementById('skillsEmpty');
-  const presets = document.getElementById('skillsPresets');
+  var list = document.getElementById('skillsList');
+  var empty = document.getElementById('skillsEmpty');
+  var presets = document.getElementById('skillsPresets');
   if(!list)return;
-  let skills = JSON.parse(localStorage.getItem('sb_skills')||'[]');
+  var skills = JSON.parse(localStorage.getItem('sb_skills')||'[]');
   if(skills.length===0){list.innerHTML='';empty.style.display='block';list.style.display='none';}
   else{empty.style.display='none';list.style.display='flex';
-    list.innerHTML = skills.map(s => `<div class="skill-item">
-      <div class="skill-emoji">${s.emoji}</div>
-      <div class="skill-info"><div class="skill-name">${s.name}</div><div>${'<span class="skill-star filled">★</span>'.repeat(s.level||3)}</div></div>
-      <button class="skill-delete" onclick="deleteSkill('${s.id}')">✕</button></div>`).join('');
+    list.innerHTML = skills.map(function(s) {
+      return "<div class=\"skill-item\">\n" +
+        "      <div class=\"skill-emoji\">" + (s.emoji) + "</div>\n" +
+        "      <div class=\"skill-info\"><div class=\"skill-name\">" + (s.name) + "</div><div>" + ('<span class=\"skill-star filled\">★</span>').repeat(s.level||3) + "</div></div>\n" +
+        "      <button class=\"skill-delete\" onclick=\"deleteSkill('" + (s.id) + "')\">✕</button></div>";
+    }).join('');
   }
-  if(presets) presets.innerHTML = skillPresets.map(p =>
-    `<div class="skill-preset${skills.some(s=>s.name===p.name)?' used':''}" onclick="addSkillPreset('${p.name}','${p.emoji}')">${p.emoji} ${p.desc}</div>`
-  ).join('');
+  if(presets) presets.innerHTML = skillPresets.map(function(p) {
+    return "<div class=\"skill-preset" + (skills.some(function(s) { return s.name===p.name; })?' used':'') + "\" onclick=\"addSkillPreset('" + (p.name) + "','" + (p.emoji) + "')\">" + (p.emoji) + " " + (p.desc) + "</div>";
+  }).join('');
 }
 
 function addSkill(){
-  const name=prompt('输入技能名称:');
+  var name =prompt('输入技能名称:');
   if(!name)return;
-  const emoji=prompt('输入表情符号:')||'⚡';
-  let skills=JSON.parse(localStorage.getItem('sb_skills')||'[]');
+  var emoji =prompt('输入表情符号:')||'⚡';
+  var skills =JSON.parse(localStorage.getItem('sb_skills')||'[]');
   skills.push({id:Date.now(),name,emoji,level:3});
   localStorage.setItem('sb_skills',JSON.stringify(skills));
   renderSkills();showToast('✅ 技能已添加');
 }
 
 function addSkillPreset(name,emoji){
-  let skills=JSON.parse(localStorage.getItem('sb_skills')||'[]');
-  if(skills.some(s=>s.name===name)){showToast('该技能已添加');return;}
+  var skills =JSON.parse(localStorage.getItem('sb_skills')||'[]');
+  if(skills.some(function(s) { return s.name===name; })){showToast('该技能已添加');return;}
   skills.push({id:Date.now(),name,emoji,level:3});
   localStorage.setItem('sb_skills',JSON.stringify(skills));
   renderSkills();showToast('✅ '+name+' 已添加');
@@ -3720,42 +3690,44 @@ function addSkillPreset(name,emoji){
 
 function deleteSkill(id){
   if(!confirm('确定删除?'))return;
-  let skills=JSON.parse(localStorage.getItem('sb_skills')||'[]');
-  skills=skills.filter(s=>s.id!=id);
+  var skills =JSON.parse(localStorage.getItem('sb_skills')||'[]');
+  skills=skills.filter(function(s) { return s.id!=id; });
   localStorage.setItem('sb_skills',JSON.stringify(skills));
   renderSkills();showToast('🗑️ 已删除');
 }
 
 // ===== 记忆系统 =====
-let currentMemoryTab='core';
+var currentMemoryTab ='core';
 function switchMemoryTab(tab){
   currentMemoryTab=tab;
-  document.querySelectorAll('.memory-tab').forEach(t=>t.classList.remove('active'));
-  document.querySelector(`.memory-tab[data-mtab="${tab}"]`).classList.add('active');
+  document.querySelectorAll('.memory-tab').forEach(function(t) { t.classList.remove('active'); });
+  document.querySelector(".memory-tab[data-mtab=\"" + (tab) + "\"]").classList.add('active');
   renderMemory();
 }
 
 function renderMemory(){
-  const content=document.getElementById('memoryContent');
-  const empty=document.getElementById('memoryEmpty');
+  var content =document.getElementById('memoryContent');
+  var empty =document.getElementById('memoryEmpty');
   if(!content)return;
-  let memories=JSON.parse(localStorage.getItem('sb_memories')||'[]');
-  const filtered=memories.filter(m=>m.type===currentMemoryTab);
+  var memories =JSON.parse(localStorage.getItem('sb_memories')||'[]');
+  var filtered =memories.filter(function(m) { return m.type===currentMemoryTab; });
   if(filtered.length===0){content.innerHTML='';empty.style.display='block';}
   else{empty.style.display='none';
-    content.innerHTML=filtered.map(m=>`<div class="memory-item">
-      <div class="memory-item-content">
-        <div class="memory-item-text">${m.text.replace(/</g,'&lt;')}</div>
-        <div class="memory-item-date">${m.date}</div>
-      </div>
-      <button class="memory-action-btn" onclick="deleteMemory('${m.id}')">🗑️</button></div>`).join('');
+    content.innerHTML = filtered.map(function(m) {
+      return "<div class=\"memory-item\">\n" +
+        "      <div class=\"memory-item-content\">\n" +
+        "        <div class=\"memory-item-text\">" + (m.text.replace(/</g,'&lt;')) + "</div>\n" +
+        "        <div class=\"memory-item-date\">" + (m.date) + "</div>\n" +
+        "      </div>\n" +
+        "      <button class=\"memory-action-btn\" onclick=\"deleteMemory('" + (m.id) + "')\">🗑️</button></div>";
+    }).join('');
   }
 }
 
 function addMemory(){
-  const text=prompt('输入记忆内容:');
+  var text =prompt('输入记忆内容:');
   if(!text)return;
-  let memories=JSON.parse(localStorage.getItem('sb_memories')||'[]');
+  var memories =JSON.parse(localStorage.getItem('sb_memories')||'[]');
   memories.unshift({id:Date.now(),type:currentMemoryTab,text,date:new Date().toLocaleString()});
   localStorage.setItem('sb_memories',JSON.stringify(memories));
   renderMemory();showToast('✅ 记忆已添加');
@@ -3763,25 +3735,29 @@ function addMemory(){
 
 function deleteMemory(id){
   if(!confirm('确定删除?'))return;
-  let memories=JSON.parse(localStorage.getItem('sb_memories')||'[]');
-  memories=memories.filter(m=>m.id!=id);
+  var memories =JSON.parse(localStorage.getItem('sb_memories')||'[]');
+  memories=memories.filter(function(m) { return m.id!=id; });
   localStorage.setItem('sb_memories',JSON.stringify(memories));
   renderMemory();showToast('🗑️ 已删除');
 }
 
 // ===== 文档库 =====
 function renderDocs(filter=''){
-  const grid=document.getElementById('docsGrid');
-  const empty=document.getElementById('docsEmpty');
+  var grid =document.getElementById('docsGrid');
+  var empty =document.getElementById('docsEmpty');
   if(!grid)return;
-  let docs=docsData.filter(d=>!filter||d.name.toLowerCase().includes(filter.toLowerCase())||d.tags.some(t=>t.includes(filter)));
-  if(docs.length===0){grid.innerHTML='';empty.style.display='block';}
-  else{empty.style.display='none';
-    grid.innerHTML=docs.map(d=>`<div class="doc-card" onclick="showToast('打开: ${d.name}')">
-      <div class="doc-card-icon">${d.icon}</div>
-      <div class="doc-name">${d.name}</div>
-      <div class="doc-meta"><span>📁 ${d.type}</span><span>🕐 ${d.date}</span></div>
-      <div class="doc-tags">${d.tags.map(t=>`<span class="doc-tag">${t}</span>`).join('')}</div></div>`).join('');
+  var docs = docsData.filter(function(d) { 
+    return !filter || d.name.toLowerCase().includes(filter.toLowerCase()) || d.tags.some(function(t) { return t.includes(filter); });
+  });
+  if(docs.length === 0) { grid.innerHTML = ''; empty.style.display = 'block'; }
+  else { empty.style.display = 'none';
+    grid.innerHTML = docs.map(function(d) {
+      return "<div class=\"doc-card\" onclick=\"showToast('打开: " + (d.name) + "')\">\n" +
+        "      <div class=\"doc-card-icon\">" + (d.icon) + "</div>\n" +
+        "      <div class=\"doc-name\">" + (d.name) + "</div>\n" +
+        "      <div class=\"doc-meta\"><span>📁 " + (d.type) + "</span><span>🕐 " + (d.date) + "</span></div>\n" +
+        "      <div class=\"doc-tags\">" + (d.tags.map(function(t) { return "<span class=\"doc-tag\">" + t + "</span>"; }).join('')) + "</div></div>";
+    }).join('');
   }
 }
 
@@ -3791,9 +3767,9 @@ function addDoc(){showToast('📝 新建文档功能开发中...');}
 
 // ===== AI 模式选择 =====
 function selectAIMode(el){
-  document.querySelectorAll('.ai-mode-option').forEach(o=>o.classList.remove('active'));
+  document.querySelectorAll('.ai-mode-option').forEach(function(o) { o.classList.remove('active'); });
   el.classList.add('active');
-  const mode = el.dataset.mode;
+  var mode = el.dataset.mode;
   document.getElementById('openaiConfigPanel').style.display = mode==='openai'?'block':'none';
   if(mode!=='openai') showToast('已切换到 '+el.querySelector('.mode-name').textContent);
 }
@@ -3801,13 +3777,13 @@ function selectAIMode(el){
 // ===== 网关控制 =====
 function restartGateway(){
   showToast('🔄 正在重启网关...', 'loading');
-  setTimeout(()=>showToast('✅ 网关重启成功', 'success'),1500);
+  setTimeout(function() { showToast('✅ 网关重启成功', 'success'); },1500);
 }
 
 // ===== 员工详情 =====
 function showEmpDetail(id){
-  const emp = employees.find(e=>e.id===id);
-  if(emp) showToast(`👤 ${emp.name} · ${emp.role}`);
+  var emp = employees.find(function(e) { return e.id===id; });
+  if(emp) showToast("👤 " + (emp.name) + " · " + (emp.role));
 }
 
 // ===== Toast =====
@@ -3861,11 +3837,10 @@ function performSearch(q){
       '<span style="font-size:20px">' + s.emoji + '</span><div><div style="font-size:14px;font-weight:600">' + s.name + '</div></div></div>';
   });
   if(docMatches.length) html += '<div style="padding:12px 20px;font-size:12px;color:var(--text-secondary);font-weight:600">文档</div>';
-  docMatches.forEach(d=>{
-    html += `<div class="search-result-item" onclick="switchTab('docs');closeSearch()">
-      <span style="font-size:20px">${d.icon}</span><div><div style="font-size:14px;font-weight:600">${d.name}</div><div style="font-size:12px;color:var(--text-secondary)">${d.date}</div></div></div>`;
+  docMatches.forEach(function(d){
+    html += "<div class=\"search-result-item\" onclick=\"switchTab('docs');closeSearch()\">\n      <span style=\"font-size:20px\">" + (d.icon) + "</span><div><div style=\"font-size:14px;font-weight:600\">" + (d.name) + "</div><div style=\"font-size:12px;color:var(--text-secondary)\">" + (d.date) + "</div></div></div>";
   });
-  if(!html) html = '<div style="padding:32px;text-align:center;color:var(--text-secondary)"><div style="font-size:36px;margin-bottom:12px">😕</div><p>没有找到 "${q}" 相关结果</p></div>';
+  if(!html) html = '<div style="padding:32px;text-align:center;color:var(--text-secondary)"><div style="font-size:36px;margin-bottom:12px">😕</div><p>没有找到 "' + q + '" 相关结果</p></div>';
 
   results.innerHTML = html;
 }
@@ -3921,7 +3896,7 @@ async function openDetailPanel(empId){
   }
   
   // Load identity data
-  const identity = await getAgentIdentity(empId);
+  var identity = await getAgentIdentity(empId);
   if (identity) {
     var statusMap = {online:'在线',busy:'工作中',offline:'离线',idle:'空闲',thinking:'思考中',working:'工作中'};
     var statusColor = {online:'var(--green)',busy:'var(--orange)',offline:'var(--gray-4)',idle:'var(--gray-4)',thinking:'var(--blue)',working:'var(--orange)'};
@@ -4754,20 +4729,20 @@ function renderLoungeZone(){
 }
 
 function updateStatusTime(){
-  const el = document.getElementById('statusTime');
+  var el = document.getElementById('statusTime');
   if(el){
-    const now = new Date();
+    var now = new Date();
     el.textContent = now.toLocaleTimeString('zh-CN',{hour:'2-digit',minute:'2-digit'});
   }
 }
 
 // ===== Live 更新模拟 =====
 function startLiveUpdates(){
-  setInterval(()=>{
+  setInterval(function() {
     if (employees.length === 0) return;
-    const idx = Math.floor(Math.random()*activities.length);
-    const statuses = ['thinking','done','idle','p1'];
-    const texts = ['正在处理任务...','✅ 任务完成','💤 等待中','⚠️ 发现问题'];
+    var idx = Math.floor(Math.random()*activities.length);
+    var statuses = ['thinking','done','idle','p1'];
+    var texts = ['正在处理任务...','✅ 任务完成','💤 等待中','⚠️ 发现问题'];
     if (activities[idx]) {
       activities[idx].status = statuses[Math.floor(Math.random()*statuses.length)];
       activities[idx].time = '刚刚';
@@ -4852,7 +4827,7 @@ function renderTaskList(){
   container.innerHTML = html;
 }
 
-document.addEventListener('DOMContentLoaded', async ()=>{
+document.addEventListener('DOMContentLoaded', async function() {
   // Initialize OpenClaw connection
   await initOpenClaw();
   
@@ -4877,24 +4852,24 @@ document.addEventListener('DOMContentLoaded', async ()=>{
 
 // Backup renderActivities for fallback
 function renderActivitiesFallback(){
-  const container = document.getElementById('liveActivities');
+  var container = document.getElementById('liveActivities');
   if(!container) return;
-  container.innerHTML = activitiesFallback.map(a=>`
-    <div class="activity-item">
-      <div class="activity-avatar" style="background:${a.gradient[0]}">${a.initial}</div>
-      <div class="activity-content">
-        <div class="activity-name">${a.name}</div>
-        <div class="activity-status">
-          <span class="status-badge ${a.status}">${a.text}</span>
-        </div>
-      </div>
-      <div class="activity-time">${a.time}</div>
-    </div>
-  `).join('');
+  container.innerHTML = activitiesFallback.map(function(a) {
+    return "<div class=\"activity-item\">\n" +
+      "      <div class=\"activity-avatar\" style=\"background:" + (a.gradient[0]) + "\">" + (a.initial) + "</div>\n" +
+      "      <div class=\"activity-content\">\n" +
+      "        <div class=\"activity-name\">" + (a.name) + "</div>\n" +
+      "        <div class=\"activity-status\">\n" +
+      "          <span class=\"status-badge " + (a.status) + "\">" + (a.text) + "</span>\n" +
+      "        </div>\n" +
+      "      </div>\n" +
+      "      <div class=\"activity-time\">" + (a.time) + "</div>\n" +
+      "    </div>";
+  }).join('');
 }
 
 // ====== 创建龙虾向导 ======
-const soulTemplates = {
+var soulTemplates = {
   assistant: {
     name: '助理型',
     emoji: '📋',
@@ -4939,7 +4914,7 @@ if(typeof AVATAR_PRESETS !== 'undefined'){
   }
 }
 
-let wizardState = {
+var wizardState = {
   step: 1,
   avatar: null,
   avatarBg: null,
@@ -4961,22 +4936,22 @@ function closeWizard() {
 }
 
 function renderWizardStep() {
-  const stepIndicators = document.querySelectorAll('.wizard-step');
-  stepIndicators.forEach((el, i) => {
-    const num = i + 1;
+  var stepIndicators = document.querySelectorAll('.wizard-step');
+  stepIndicators.forEach(function(el, i) {
+    var num = i + 1;
     el.classList.remove('active', 'done');
     if (num === wizardState.step) el.classList.add('active');
     else if (num < wizardState.step) el.classList.add('done');
   });
   
-  const steps = document.querySelectorAll('.wizard-step-content');
-  steps.forEach((el, i) => {
+  var steps = document.querySelectorAll('.wizard-step-content');
+  steps.forEach(function(el, i) {
     el.style.display = i + 1 === wizardState.step ? 'block' : 'none';
   });
   
-  const prevBtn = document.getElementById('wizardPrevBtn');
-  const nextBtn = document.getElementById('wizardNextBtn');
-  const createBtn = document.getElementById('wizardCreateBtn');
+  var prevBtn = document.getElementById('wizardPrevBtn');
+  var nextBtn = document.getElementById('wizardNextBtn');
+  var createBtn = document.getElementById('wizardCreateBtn');
   
   prevBtn.style.display = wizardState.step > 1 ? 'inline-flex' : 'none';
   nextBtn.style.display = wizardState.step < 3 ? 'inline-flex' : 'none';
@@ -5037,7 +5012,7 @@ function prevStep() {
 
 function selectSoulTemplate(key) {
   wizardState.soulTemplate = key;
-  document.querySelectorAll('.soul-template-card').forEach(el => el.classList.remove('selected'));
+  document.querySelectorAll('.soul-template-card').forEach(function(el) { el.classList.remove('selected'); });
   event.currentTarget.classList.add('selected');
   updateWizardPreview();
 }
@@ -5067,15 +5042,15 @@ function updateWizardPreview() {
 async function confirmCreateAgent() {
   wizardState.model = document.getElementById('wizardModelSelect').value;
   
-  const tmpl = soulTemplates[wizardState.soulTemplate];
-  const soul = tmpl.soul.replace(/{name}/g, wizardState.name).replace(/{role}/g, wizardState.role);
-  const identity = tmpl.identity.replace(/{name}/g, wizardState.name).replace(/{role}/g, wizardState.role);
+  var tmpl = soulTemplates[wizardState.soulTemplate];
+  var soul = tmpl.soul.replace(/{name}/g, wizardState.name).replace(/{role}/g, wizardState.role);
+  var identity = tmpl.identity.replace(/{name}/g, wizardState.name).replace(/{role}/g, wizardState.role);
   
   showToast('正在创建 Agent...', 'loading');
   
   try {
     if (window.openclawConnected && typeof openclaw !== 'undefined' && openclaw.agents) {
-      const result = await openclaw.send('agents.create', {
+      var result = await openclaw.send('agents.create', {
         name: wizardState.name,
         role: wizardState.role,
         model: wizardState.model,
@@ -5087,7 +5062,7 @@ async function confirmCreateAgent() {
       showToast('🎉 Agent 创建成功！', 'success');
       
       // Add to local array
-      const newAgent = {
+      var newAgent = {
         id: result.id || 'agent_' + Date.now(),
         name: wizardState.name,
         role: wizardState.role,
@@ -5103,7 +5078,7 @@ async function confirmCreateAgent() {
       employees.push(newAgent);
     } else {
       // Offline mode simulation
-      const newAgent = {
+      var newAgent = {
         id: 'emp_' + Date.now(),
         name: wizardState.name,
         role: wizardState.role,
@@ -5125,7 +5100,7 @@ async function confirmCreateAgent() {
     
     // 尝试同步到后端
     try {
-      const newEmp = employees[employees.length - 1];
+      var newEmp = employees[employees.length - 1];
       await authFetch('/api/agents', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
